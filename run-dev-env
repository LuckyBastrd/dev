#!/usr/bin/env bash
set -e

# --------------------------------------------------
# Resolve source directory (repo root)
# --------------------------------------------------
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CHEZMOI_SOURCE="$SCRIPT_DIR"
CURRENT_SOURCE=$(chezmoi source-path 2>/dev/null || echo "")

# --------------------------------------------------
# Utilities
# --------------------------------------------------
has_cmd() {
  command -v "$1" >/dev/null 2>&1
}

# --------------------------------------------------
# Install Homebrew (macOS only)
# --------------------------------------------------
install_brew() {
  [[ "$(uname -s)" == "Darwin" ]] || {
    echo "Error: Homebrew install attempted on non-macOS system"
    exit 1
  }
  echo "🍺 Installing Homebrew..."
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  if [[ -x /opt/homebrew/bin/brew ]]; then
    eval "$(/opt/homebrew/bin/brew shellenv)"
  elif [[ -x /usr/local/bin/brew ]]; then
    eval "$(/usr/local/bin/brew shellenv)"
  fi
}

# --------------------------------------------------
# Install chezmoi
# --------------------------------------------------
install_chezmoi() {
  local os_name="$1"
  echo "📦 Installing chezmoi..."
  case "$os_name" in
    arch)
      sudo pacman -S --noconfirm chezmoi
      ;;
    mac)
      brew install chezmoi
      ;;
    *)
      echo "Error: Unsupported OS for chezmoi installation"
      exit 1
      ;;
  esac
}

# --------------------------------------------------
# Prompt to apply
# --------------------------------------------------
prompt_apply() {
  read -r -p "Apply dotfiles now? (y/N): " answer
  case "$answer" in
    [Yy]*)
      echo "✅ Applying dotfiles..."
      chezmoi apply
      ;;
    *)
      echo "ℹ️  Skipped. Run 'chezmoi apply' later."
      ;;
  esac
}

# --------------------------------------------------
# Main
# --------------------------------------------------
main() {
  local os_name apply_mode="prompt"

  # Parse args
  while [[ $# -gt 0 ]]; do
    case "$1" in
      --apply)
        apply_mode="yes"
        shift
        ;;
      --no-apply)
        apply_mode="no"
        shift
        ;;
      --help|-h)
        echo "Usage: $0 [OPTIONS]"
        echo ""
        echo "Options:"
        echo "  --apply              Apply dotfiles without prompting"
        echo "  --no-apply           Skip applying dotfiles"
        echo "  --help, -h           Show this help message"
        echo ""
        echo "Examples:"
        echo "  $0                          # Interactive mode"
        echo "  $0 --apply                  # Auto-apply"
        exit 0
        ;;
      *)
        echo "Unknown option: $1"
        echo "Use --help for usage information"
        exit 1
        ;;
    esac
  done

  # OS detection
  case "$(uname -s)" in
    Linux*)
      [[ -f /etc/arch-release ]] || {
        echo "Error: Unsupported Linux distribution"
        exit 1
      }
      os_name="arch"
      ;;
    Darwin*)
      os_name="mac"
      ;;
    *)
      echo "Error: Unsupported operating system"
      exit 1
      ;;
  esac

  # macOS: ensure Homebrew
  if [[ "$os_name" == "mac" ]] && ! has_cmd brew; then
    install_brew
  fi

  # Ensure chezmoi
  if ! has_cmd chezmoi; then
    install_chezmoi "$os_name"
  fi

  # Init only if needed
  if [[ "$CURRENT_SOURCE" != "$CHEZMOI_SOURCE" ]]; then
    echo "📁 Initializing chezmoi from: $CHEZMOI_SOURCE"
    chezmoi init --source "$CHEZMOI_SOURCE"
  else
    echo "✅ chezmoi already configured for this source directory"
  fi

  # Apply behavior
  case "$apply_mode" in
    yes)
      echo "✅ Applying dotfiles..."
      chezmoi apply
      ;;
    no)
      echo "ℹ️  Skipping apply (--no-apply)"
      ;;
    prompt)
      prompt_apply
      ;;
  esac
}

main "$@"
